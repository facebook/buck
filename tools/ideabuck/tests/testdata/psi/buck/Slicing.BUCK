# Slicing and subscripts

words = ['one', 'two', 'three', 'four', 'five']
words[1]

words[1:]
words[:3]
words[1:3]

words[::2]
words[1::-2]
words[:3:2]
words[1:3:2]

scores = {'one': 1, 'two': 2, 'three': 3, ('fo', 'ur'): 4}
scores['one']
scores['fo', 'ur']

scores['new'] = len(words[::-1])
