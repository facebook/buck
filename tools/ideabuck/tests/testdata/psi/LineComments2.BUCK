# Line comments in a nested suite

def foo(x):
    # comment in block should set indentation level
    y = [x,
        # indentation
      # doesn't
    # matter
  # inside
# parens or braces
         ] # or here
    # outside parens/braces, comment indentation matters again
# This less-indented comment should be at same level as next statement
bar = foo