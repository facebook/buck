[second_section]
some_property = some_value

[ignored_section]
    dotted.value = value with dot in name
    short_value = short test value
    long_value = multiline value with \n \
      and \r\n chars in it

[repositories]
    secondary = secondary

[python#py2]
interpreter=python2

[python#py3]
interpreter=python3

<file:.buckconfig.include>

# Optional include
<?file:.buckconfig.does.not.exist>

[included_inline_section]
  <file:.buckconfig.include.section>
  # This value overrides included key.
  included_key = real value
