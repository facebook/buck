load("//:defs.bzl", "my_rule")

my_rule(
    name = "no_defaults",
    int = 1,
    string = "foo_value",
    string_list = [
        "foo",
        "baz",
    ],
)

my_rule(
    name = "defaults",
)
