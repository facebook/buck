from setuptools import setup

setup(
    name='package',
    version='0.1',
    description="magic. it's full of magic.",
    author='buck',
    author_email='buck@buck.buck',
    packages=['package'],
)
