export_file(
  name = 'secondary-Info.plist',
  visibility = ['PUBLIC'],
)

apple_library(
  name = 'applelib',
  srcs = glob(['*.m']),
  info_plist = 'secondary-Info.plist',
  exported_headers = glob(['*.h']),
  visibility = ['PUBLIC'],
)
