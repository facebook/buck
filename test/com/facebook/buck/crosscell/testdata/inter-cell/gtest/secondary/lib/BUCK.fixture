cxx_library(
  name = 'cxxlib',
  header_namespace = 'lib',
  exported_headers = glob(['*.h']),
  srcs = glob(['*.cpp']),
  compiler_flags = [
    '-g',
    '-std=c++11',
  ],
  visibility = ['PUBLIC'],
)
