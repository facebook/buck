[buildfile]
    name = TARGETS
[ndk]
    # Use the system runtime so that we don't have to deal with `buck install` pushing the c++
    # runtime.
    cxx_runtime = system
    ndk_version = 17

[android]
    secondary_dex_weight_limit = 2000
