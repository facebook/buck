genrule(
  name = 'gen-src',
  bash = 'mkdir -p $OUT && touch $OUT',
  out = 'subdir/src',
)

genrule(
  name = 'gen',
  bash = 'cp -r subdir/src $OUT',
  srcs = [
    ':gen-src',
  ],
  out = 'out',
)
