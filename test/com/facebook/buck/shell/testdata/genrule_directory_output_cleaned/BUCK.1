genrule(
  name = 'mkdir',
  cmd = 'mkdir $OUT && echo something > $OUT/one',
  out = 'directory',
)

genrule(
  name = 'mkdir_another',
  cmd = 'mkdir $OUT && echo something > $OUT/file',
  out = 'another_directory',
)
