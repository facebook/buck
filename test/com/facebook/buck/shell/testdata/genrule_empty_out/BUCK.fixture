genrule(
    name = "genrule",
    out = "",
    cmd = "echo something > $OUT",
)

genrule(
    name = "genrule_outs",
    outs = {
        "output": [
            "",
        ],
    },
    cmd = "echo something > $OUT",
)
