java_library(
    name = "external",
    srcs = glob(["*.java"]),
    visibility = [
        "PUBLIC",
    ],
    deps = [
        # Don't add deps; this is a very lightweight library that is intended for use by annotation
        # processors
    ],
)
